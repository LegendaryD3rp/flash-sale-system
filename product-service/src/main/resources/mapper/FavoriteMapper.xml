<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flashsale.productservice.mapper.FavoriteMapper">

    <resultMap id="favoriteProductMap" type="com.flashsale.productservice.vo.FavoriteProductVO">
        <id property="id" column="fid"/>
        <result property="productId" column="product_id"/>
        <result property="name" column="name"/>
        <result property="price" column="price"/>
        <result property="imageUrl" column="image_url"/>
        <result property="category" column="category"/>
        <result property="status" column="status"/>
        <result property="favoriteTime" column="favorite_time"/>
    </resultMap>

    <select id="selectFavoriteProducts" resultMap="favoriteProductMap">
        SELECT
            f.id AS fid,
            f.product_id,
            p.name,
            p.price,
            p.image_url,
            p.category,
            p.status,
            f.created_at AS favorite_time
        FROM user_favorite f
        JOIN product p ON f.product_id = p.id
        WHERE f.user_id = #{userId}
        ORDER BY f.created_at DESC
    </select>

</mapper>
